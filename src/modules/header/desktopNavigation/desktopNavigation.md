```js
  <DesktopNavigation
    position='static'
    loggedIn
    accountLinks={require('./defaultProps').accountLinks}/>
  <div style={{height: '60rem'}} />
```

```js
  <DesktopNavigation
    position='static'
    loggedIn
    isSubscriptionMember
    accountLinks={require('./defaultProps').accountLinks}/>
  <div style={{height: '60rem'}} />
```

```js
  <DesktopNavigation
    position='static'
    accountLinks={require('./defaultProps').accountLinks}/>
  <div style={{height: '60rem'}} />
```

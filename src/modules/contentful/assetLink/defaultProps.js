import { default as responsiveProps } from '../responsive/images/defaultProps'

const defaultProps = {
  fields: {
    asset: responsiveProps,
    destination: '/shop/boys/all'
  },
  sys: {
    contentType: {
      sys: {
        id: 'assetLink'
      }
    }
  }
}

export default defaultProps
